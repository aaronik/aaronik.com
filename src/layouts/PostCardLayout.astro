---
const { frontmatter } = Astro.props
---

<li class="rounded-md overflow-hidden hover:brightness-125 max-w-sm bg-gray-800 shadow-lg list-none transition-all duration-100">
    <a href={frontmatter.href} class="block no-underline p-5">
        <h3 class="text-lg font-semibold mb-2 text-white p-1">{frontmatter.title}</h3>
        <img src={frontmatter.imgSrc} alt={frontmatter.title} class="w-full h-48 object-cover p-1" />
        <p class="text-gray-400 text-sm p-1">{frontmatter.description}</p>
    </a>
</li>
