---
import Logo from '../components/Logo.astro';

const navItems = [
    { linkText: 'Projects', href: '#projects' },
    { linkText: 'Blog', href: '#blog' }
];
---

<nav class="flex flex-wrap items-center gap-4 mb-6 p-3 justify-between sticky top-0 z-50 bg-gray-900/80 backdrop-blur-md font-mono rounded-lg">
    <a href="/">
        <Logo />
    </a>
    {
        !!navItems?.length && (
            <ul class="flex flex-wrap gap-x-4 gap-y-1">
                {navItems.map((item) => (
                    <li>
                        <a
                            href={item.href}
                            class="inline-block px-1.5 py-1 sm:px-3 sm:py-2 transition-transform transform hover:scale-105 hover:text-green-500 duration-300 bg-black bg-opacity-70 border border-green-500 rounded-lg shadow-lg hover:shadow-xl"
                        >
                            {item.linkText}
                        </a>
                    </li>
                ))}
            </ul>
        )
    }
</nav>

<style>
    a {
        position: relative;
        color: #00ffcc;
        text-decoration: none;
        font-family: 'Orbitron', sans-serif;
    }
    a::before {
        content: '';
        position: absolute;
        left: 0;
        bottom: -2px;
        width: 100%;
        height: 2px;
        background: linear-gradient(to right, #00ffcc, #00ccff);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }
    a:hover::before {
        transform: scaleX(0.7);
    }
</style>
